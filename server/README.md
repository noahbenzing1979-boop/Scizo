# Server

Server scaffold for future authoritative combat resolution, run state, room generation, and party synchronization.

The current browser client owns the playable loop. The server remains intentionally small until the game needs multiplayer, account state, or validated run history.
